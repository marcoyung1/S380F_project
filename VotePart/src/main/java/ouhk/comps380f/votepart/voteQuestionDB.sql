CREATE TABLE QuestionDB (
    QuestionID int NOT NULL,
    Question varchar(255) NOT NULL,
    ansA varchar(50),
    ansB varchar(50),
    ansC varchar(50),
    ansD varchar(50),
    PRIMARY KEY (questionID)
);